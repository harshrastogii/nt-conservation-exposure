# data/ — raw licensed source layers are large and reproducible; not tracked.
# The two DERIVED analysis inputs ARE tracked (CC BY 4.0, attributed): see !exceptions below.
data/
!data/hex_master.gpkg
!data/roper_intersection.gpkg
# python
__pycache__/
*.pyc
.ipynb_checkpoints/
# os
.DS_Store
# keep figures and QA outputs tracked (they're derived, small, illustrative)
!figures/

# unrelated local app + non-portable pip freeze
app/
100-parts

# local unrelated app
app/

# macOS
.DS_Store

# regenerable heavy sensitivity outputs (duplicate figure + per-hex intermediate)
analysis/outputs_sensitivity/fig_hotspot_persistence.png
analysis/outputs_sensitivity/hotspot_persistence.csv
